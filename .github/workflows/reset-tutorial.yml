name: Reset Tutorial

on:
  workflow_dispatch:

jobs:
  reset:
    uses: jgutierrezdtt/security-tutorial-base/.github/workflows/reusable-reset.yml@main
    with:
      tutorial-name: SAST OpenGrep Professional
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
